{
  "name": "@drinks/api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc --project tsconfig.json",
    "start": "node dist/server.js",
    "lint": "echo api app does not need lint step",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@drinks/types": "file:../../packages/types",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "tsx": "^4.20.6"
  }
}

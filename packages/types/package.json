{
  "name": "@drinks/types",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo types package does not need build step",
    "lint": "echo types package does not need lint step",
    "typecheck": "tsc --project tsconfig.json"
  }
}
